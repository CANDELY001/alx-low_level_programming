#include "main.h"
/**
 * more_numbers - Write a function that prints 10 times the numbers, from 0 to 14
 */
void more_numbers(void)
{
	int c,i;

	for (c = '0'; i <= '9'; i++)
	{
		for (c = '0'; c <= '14'; c++)
		{
			_putchar(n);
		}
	}
	putchar('\n');
}
